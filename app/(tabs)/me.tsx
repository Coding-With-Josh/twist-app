import { DarkTheme } from "@/themes";
import { View, Text } from "react-native";

const Profile = () => {
  return (
    <View>
      <Text style={{ color: DarkTheme.colors.text}}>Profile</Text>
    </View>
  );
};

export default Profile;