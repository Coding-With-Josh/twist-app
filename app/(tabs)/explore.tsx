import { DarkTheme } from "@/themes";
import { View, Text } from "react-native";

const Explore = () => {
  return (
    <View>
      <Text style={{color: DarkTheme.colors.text}}>Explore</Text>
    </View>
  );
};

export default Explore;